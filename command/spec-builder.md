---
description: Implements task from spec-planner and reviews code
---

Implements a task planned by `/spec-planner` and calls code-reviewer at the end.

First, invoke the skill tool to load the spec-builder skill:

```
skill({ name: 'spec-builder' })
```

Then follow the skill instructions to implement the task and review code.

<user-request>
$ARGUMENTS
</user-request>
