---
description: Analyze task and generate prd.json with task breakdown
---

Analyze a task description and generate a prd.json file with user story breakdown.

First, invoke the skill tool to load the spec-planner skill:

```
skill({ name: 'spec-planner' })
```

Then follow the skill instructions to analyze and generate the PRD.

<user-request>
$ARGUMENTS
</user-request>
